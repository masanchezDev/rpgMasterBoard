{
  "name": "rpg-master-board",
  "version": "1.0.0",
  "description": "RPG Master Board - Game Master control panel for tabletop RPG sessions",
  "scripts": {
    "dev": "concurrently \"pnpm run server\" \"pnpm run client\"",
    "server": "cd server && pnpm run dev",
    "client": "cd client && pnpm run dev",
    "install:all": "pnpm install && cd server && pnpm install && cd ../client && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
